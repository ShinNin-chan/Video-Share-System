{% extends "banner_base.html" %}

{% block banner %}

<h3> <a href="/timeline/all">All Videos.</a></h3>
<h3> <a href="/timeline">Videos from friends.</a></h3>

	<p>
	{{page_owner.username}},{{page_owner.id}}
	{% if flag ==  2 %} 
	<form action = '' method = 'get'>
		<input type = 'submit' value = 'Follow' />
	</form>
	{% elif flag == 1 %}
		followed
	{% endif %}
	search
	</p>
    <br/>
	
		{% for video in videos %}
		<p>
			<a href="personalPage/{{video.owner.id}}">{{video.owner.username}}</a> {{video.uploadtime}}
			<br><a href="videoplay/{{video.id}}">{{video.title}}</a>
			<hr>
		</p>
		{% endfor %}
	
{% endblock %}
